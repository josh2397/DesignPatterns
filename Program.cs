// Create a template for each design pattern
// Create 2 examples for each
// Ensure that all follow solid principles
// Create unit tests for all patterns

// Be able to interface with each pattern, specifying parameters where applicable for creation patterns etc.

class Program {

    static void Main(string[] args)
    {
        // Get input for which pattern to interact with

        Console.WriteLine("Enter the name of the pattern to interact with or enter the corresponding number:\n");
        Console.WriteLine("1: Factory");
        Console.WriteLine("exit");
    
    }

    static void CallDesignPattern(string input)
    {
        switch (input)
        {
            case "factory":
            case "1":
                
                break;

        }
    }
}



